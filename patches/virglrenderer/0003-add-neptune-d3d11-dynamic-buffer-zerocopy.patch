diff --git a/src/neptune/npt_context.c b/src/neptune/npt_context.c
index b7aba5df..f65359e7 100644
--- a/src/neptune/npt_context.c
+++ b/src/neptune/npt_context.c
@@ -22,6 +22,7 @@
 #include "npt_overrides.h"
 #include "npt_profile.h"
 #include "npt_queue.h"
+#include "npt_resource.h"
 #include "npt_ring.h"
 #include "npt_shared.h"
 #include "neptune-protocol/npt_protocol_host_dispatch.h"
@@ -93,6 +94,43 @@ npt_context_free_detached_resource(struct npt_resource *res)
    npt_resource_free(res);
 }

+struct npt_resource *
+npt_context_pin_resource(struct npt_context *ctx, uint32_t res_id)
+{
+   if (!ctx || !ctx->resource_table)
+      return NULL;
+
+   mtx_lock(&ctx->resource_mutex);
+   const struct hash_entry *entry =
+      _mesa_hash_table_search(ctx->resource_table, &res_id);
+   struct npt_resource *res = entry ? entry->data : NULL;
+   if (res)
+      res->pin_count++;
+   mtx_unlock(&ctx->resource_mutex);
+
+   return res;
+}
+
+void
+npt_context_unpin_resource(struct npt_context *ctx,
+                           struct npt_resource *res)
+{
+   if (!ctx || !res)
+      return;
+
+   mtx_lock(&ctx->resource_mutex);
+   assert(res->pin_count > 0);
+   res->pin_count--;
+   const bool free_now =
+      res->zombie &&
+      res->pin_count == 0 &&
+      res->heap_import_count == 0;
+   mtx_unlock(&ctx->resource_mutex);
+
+   if (free_now)
+      npt_context_free_detached_resource(res);
+}
+
 /* Overrides are set only where the default dispatcher cannot cope:
  * top-level functions (no _self for dlsym to bind against),
  * shared-HANDLE rejection, and feedback lifecycle hooks.  All other
@@ -434,6 +472,29 @@ npt_context_lookup_object(struct npt_context *ctx,
    return obj;
 }

+void *
+npt_context_lookup_object_addref(struct npt_context *ctx,
+                                 uint64_t id,
+                                 npt_object_type expected)
+{
+   if (!ctx || !id || !ctx->object_table)
+      return NULL;
+
+   void *host_ptr = NULL;
+   mtx_lock(&ctx->object_mutex);
+   const struct hash_entry *entry =
+      _mesa_hash_table_search(ctx->object_table, &id);
+   const struct npt_object *obj = entry ? entry->data : NULL;
+   if (obj && npt_object_type_is_compatible(obj->type, expected)) {
+      /* Keep lookup and AddRef under the same lock: COM_RELEASE cannot
+       * remove/drop the table's object ref between the two operations. */
+      host_ptr = obj->host_ptr;
+      npt_com_add_ref(host_ptr);
+   }
+   mtx_unlock(&ctx->object_mutex);
+   return host_ptr;
+}
+
 void
 npt_context_release_object(struct npt_context *ctx, uint64_t guest_id)
 {
@@ -789,6 +850,10 @@ npt_context_destroy(struct npt_context *ctx)
    free(ctx->destroyed_ring_ids);
    mtx_destroy(&ctx->deferred_mutex);

+   /* Close real DXMT Maps while their pinned COM objects are still alive.
+    * Ring threads are already stopped, so sync-map state is quiescent. */
+   npt_resource_drain_external_maps(ctx);
+
    /* Guests are expected to release every COM object before context
     * teardown.  Free any straggler entries to plug a guest leak. */
    hash_table_foreach(ctx->object_table, entry) {
@@ -1436,19 +1501,20 @@ npt_context_destroy_resource(struct npt_context *ctx, uint32_t res_id)
       npt_ring_destroy(ring);
    }

-   /* A live ID3D12Heap import still aliases this mapping, so munmapping
-    * it now is undefined under the host graphics driver.  Park the
-    * detached entry as a zombie for the last import to free. */
+   /* The resource is already detached from resource_table.  Existing
+    * users may still hold either a long-lived heap import or a transient
+    * command pin; in either case the backing mapping/fd must remain live. */
    mtx_lock(&ctx->resource_mutex);
    const uint32_t imports = res->heap_import_count;
-   if (imports > 0)
+   const uint32_t pins = res->pin_count;
+   if (imports > 0 || pins > 0)
       res->zombie = true;
    mtx_unlock(&ctx->resource_mutex);

-   if (imports > 0) {
-      npt_log("destroy_resource: res %u still imported by %u D3D12 heap(s); "
-              "DEFERRING munmap until heap release (zombie)",
-              res_id, imports);
+   if (imports > 0 || pins > 0) {
+      npt_log("destroy_resource: res %u still pinned "
+              "(heap_imports=%u transient=%u); DEFERRING free (zombie)",
+              res_id, imports, pins);
       return;
    }

diff --git a/src/neptune/npt_context.h b/src/neptune/npt_context.h
index b334af70..a690d65b 100644
--- a/src/neptune/npt_context.h
+++ b/src/neptune/npt_context.h
@@ -43,6 +43,9 @@ struct npt_sync_map_entry {
    void    *mapped_data;   /* host pointer returned by D3D11/12 Map */
    uint32_t mapped_size;
    bool     persistent;    /* D3D12 persistent map: no Unmap expected */
+   bool     external;      /* DXMT buffer aliases guest SHM */
+   void    *external_ctx;  /* owned COM ref while guest CPU map is live */
+   void    *external_resource; /* owned COM ref while guest CPU map is live */
 };

 struct npt_resource {
@@ -68,6 +71,10 @@ struct npt_resource {
     * marks the entry `zombie` instead and the last import released
     * completes the free.  Both fields are guarded by resource_mutex. */
    uint32_t heap_import_count;
+   /* Short-lived users of this npt_resource outside resource_mutex.
+    * destroy_resource detaches immediately but defers munmap/free until
+    * both import and transient pins have drained. */
+   uint32_t pin_count;
    bool zombie;
 };

@@ -282,6 +289,16 @@ npt_context_import_resource(struct npt_context *ctx,
 void
 npt_context_destroy_resource(struct npt_context *ctx, uint32_t res_id);

+/* Atomically look up a resource and hold its backing mapping/fd alive
+ * after resource_mutex is dropped.  A detached zombie is freed by the
+ * last transient/import pin. */
+struct npt_resource *
+npt_context_pin_resource(struct npt_context *ctx, uint32_t res_id);
+
+void
+npt_context_unpin_resource(struct npt_context *ctx,
+                           struct npt_resource *res);
+
 /* Free a resource already detached from resource_table, completing a
  * munmap that was deferred while it was pinned. */
 void
@@ -418,6 +435,12 @@ npt_context_lookup_object(struct npt_context *ctx,
                           uint64_t id,
                           npt_object_type expected);

+/* Lookup and AddRef atomically with respect to object-table removal. */
+void *
+npt_context_lookup_object_addref(struct npt_context *ctx,
+                                 uint64_t id,
+                                 npt_object_type expected);
+
 /* COM_RELEASE coordination: drop the feedback entry (if any), unmap
  * the object_table entry, then drop the host-library ref via
  * IUnknown::Release.  Stray RELEASE on an unregistered id is silent. */
diff --git a/src/neptune/npt_dispatch.c b/src/neptune/npt_dispatch.c
index 30787164..f74f84b0 100644
--- a/src/neptune/npt_dispatch.c
+++ b/src/neptune/npt_dispatch.c
@@ -31,6 +31,35 @@ npt_dispatch_is_ring_dispatch(const struct npt_context *ctx,
    return dispatch->decoder != &ctx->decoder;
 }

+static void
+npt_dispatch_bind_d3d11_buffer_shmem(struct npt_context *ctx,
+                                     struct npt_cs_decoder *dec,
+                                     struct npt_cs_encoder *enc,
+                                     const struct npt_command_header *header)
+{
+   struct npt_cmd_bind_d3d11_buffer_shmem cmd;
+   cmd.header = *header;
+   npt_cs_decoder_read(dec, sizeof(cmd) - sizeof(cmd.header),
+                       &cmd.shmem_res_id,
+                       sizeof(cmd) - sizeof(cmd.header));
+   if (npt_cs_decoder_get_fatal(dec))
+      return;
+
+   HRESULT hr = npt_resource_bind_d3d11_buffer_shmem(
+      ctx, header->object_id, cmd.shmem_res_id, cmd.byte_size,
+      cmd.cookie, cmd.flags);
+
+   struct npt_cmd_bind_d3d11_buffer_shmem_reply reply = { 0 };
+   reply.header.cmd_type = header->cmd_type;
+   reply.header.cmd_return = (uint32_t)(int32_t)hr;
+   if (header->cmd_flags & NPT_CMD_FLAG_REPLY) {
+      if (npt_cs_encoder_acquire(enc)) {
+         npt_cs_encoder_write(enc, sizeof(reply), &reply, sizeof(reply));
+         npt_cs_encoder_release(enc);
+      }
+   }
+}
+
 static void
 npt_dispatch_set_reply_stream(struct npt_context *ctx,
                               struct npt_cs_decoder *dec,
@@ -355,7 +384,8 @@ npt_dispatch_map_resource(struct npt_context *ctx,
       cmd.read_range_begin, cmd.read_range_end,
       cmd.byte_size, cmd.mip_height, cmd.mip_depth,
       cmd.shmem_offset,
-      &reply.row_pitch, &reply.depth_pitch, &reply.mapped_size);
+      &reply.row_pitch, &reply.depth_pitch, &reply.mapped_size,
+      &reply.external_cookie);

    if (header->cmd_flags & NPT_CMD_FLAG_REPLY) {
       if (npt_cs_encoder_acquire(enc)) {
@@ -679,6 +709,9 @@ npt_dispatch_subgroup_resource(struct npt_context *ctx,
    case NPT_TRANSPORT_RESOURCE_CREATE_HEAP_FROM_SHMEM:
       npt_dispatch_create_heap_from_shmem(ctx, dec, enc, header);
       return true;
+   case NPT_TRANSPORT_RESOURCE_BIND_D3D11_BUFFER_SHMEM:
+      npt_dispatch_bind_d3d11_buffer_shmem(ctx, dec, enc, header);
+      return true;
    default:
       return false;
    }
diff --git a/src/neptune/npt_heap12.c b/src/neptune/npt_heap12.c
index e5271764..367e89bd 100644
--- a/src/neptune/npt_heap12.c
+++ b/src/neptune/npt_heap12.c
@@ -64,7 +64,9 @@ npt_heap12_unpin(struct npt_context *ctx, struct npt_resource *res)
    mtx_lock(&ctx->resource_mutex);
    assert(res->heap_import_count > 0);
    res->heap_import_count--;
-   const bool free_now = res->zombie && res->heap_import_count == 0;
+   const bool free_now = res->zombie &&
+                         res->heap_import_count == 0 &&
+                         res->pin_count == 0;
    const uint32_t res_id = res->res_id;
    mtx_unlock(&ctx->resource_mutex);

diff --git a/src/neptune/npt_library.c b/src/neptune/npt_library.c
index acba38b4..d74f531a 100644
--- a/src/neptune/npt_library.c
+++ b/src/neptune/npt_library.c
@@ -108,6 +108,24 @@ npt_library_load_embedder_api(struct npt_d3d_library *lib)
          ((union { void *p;
                    PFN_npt_lib_darwin_open_existing_heap_from_fd f; }){
             .p = npt_library_sym(mod, name) }).f;
+
+      if (lib->backend == NPT_BACKEND_DXMT) {
+         lib->pfn_dxmt_d3d11_buffer_bind_external_fd =
+            ((union { void *p;
+                      PFN_npt_lib_dxmt_d3d11_buffer_bind_external_fd f; }){
+               .p = npt_library_sym(mod,
+                                    "dxmt_d3d11_buffer_bind_external_fd") }).f;
+         lib->pfn_dxmt_d3d11_buffer_is_external =
+            ((union { void *p;
+                      PFN_npt_lib_dxmt_d3d11_buffer_is_external f; }){
+               .p = npt_library_sym(mod,
+                                    "dxmt_d3d11_buffer_is_external") }).f;
+         lib->pfn_dxmt_d3d11_buffer_external_cookie =
+            ((union { void *p;
+                      PFN_npt_lib_dxmt_d3d11_buffer_external_cookie f; }){
+               .p = npt_library_sym(mod,
+                                    "dxmt_d3d11_buffer_external_cookie") }).f;
+      }
       return;
    }
    npt_log("backend embedder API (dmn_/dxmt_event_*) not found");
diff --git a/src/neptune/npt_library.h b/src/neptune/npt_library.h
index 250d57f2..af185902 100644
--- a/src/neptune/npt_library.h
+++ b/src/neptune/npt_library.h
@@ -71,6 +71,13 @@ typedef HRESULT (*PFN_npt_lib_darwin_open_existing_heap_from_fd)(
    void *device, int fd, uint64_t offset, uint64_t size,
    uint32_t heap_type, uint32_t heap_flags, const GUID *iid, void **heap);

+typedef int32_t (*PFN_npt_lib_dxmt_d3d11_buffer_bind_external_fd)(
+   void *buffer, int fd, uint64_t backing_length,
+   uint32_t cookie, uint32_t flags);
+typedef int (*PFN_npt_lib_dxmt_d3d11_buffer_is_external)(void *buffer);
+typedef int (*PFN_npt_lib_dxmt_d3d11_buffer_external_cookie)(
+   void *buffer, void *mapped_ptr, uint32_t *out_cookie);
+
 struct npt_d3d_library {
    void *d3d11_module;
    void *dxgi_module;
@@ -93,6 +100,12 @@ struct npt_d3d_library {
    /* Optional, darwin: same contract as the vkd3d dmabuf twin below.
     * NULL => CREATE_HEAP_FROM_SHMEM fails cleanly (sync-map fallback). */
    PFN_npt_lib_darwin_open_existing_heap_from_fd pfn_darwin_open_existing_heap_from_fd;
+   PFN_npt_lib_dxmt_d3d11_buffer_bind_external_fd
+      pfn_dxmt_d3d11_buffer_bind_external_fd;
+   PFN_npt_lib_dxmt_d3d11_buffer_is_external
+      pfn_dxmt_d3d11_buffer_is_external;
+   PFN_npt_lib_dxmt_d3d11_buffer_external_cookie
+      pfn_dxmt_d3d11_buffer_external_cookie;
    enum npt_backend_kind backend;

    /* NPT_WA_* bits describing the workarounds the loaded backend needs, set in
diff --git a/src/neptune/npt_resource.c b/src/neptune/npt_resource.c
index 279bc4de..70ffc8c1 100644
--- a/src/neptune/npt_resource.c
+++ b/src/neptune/npt_resource.c
@@ -16,6 +16,7 @@

 #include "npt_com.h"
 #include "npt_context.h"
+#include "npt_library.h"
 #include "npt_transport_defs.h"

 #include "neptune-protocol/npt_protocol_host_dispatch_types.h"
@@ -213,6 +214,76 @@ npt_sync_map_remove(struct npt_context *ctx, struct npt_sync_map_entry *e)
       ctx->sync_maps.entries[--ctx->sync_maps.count];
 }

+static void
+npt_external_map_close(struct npt_sync_map_entry *entry)
+{
+   if (!entry || !entry->external || !entry->external_ctx ||
+       !entry->external_resource)
+      return;
+
+   PFN_ID3D11DeviceContext_Unmap unmap_fn =
+      NPT_COM_VTBL_FUNC(PFN_ID3D11DeviceContext_Unmap,
+                        npt_com_vtable(entry->external_ctx),
+                        NPT_VTBL_ID3D11DeviceContext_Unmap);
+   unmap_fn(entry->external_ctx, entry->external_resource,
+            entry->subresource);
+
+   npt_com_release(entry->external_resource);
+   npt_com_release(entry->external_ctx);
+   entry->external_resource = NULL;
+   entry->external_ctx = NULL;
+}
+
+void
+npt_resource_drain_external_maps(struct npt_context *ctx)
+{
+   uint32_t i = 0;
+   while (i < ctx->sync_maps.count) {
+      struct npt_sync_map_entry *entry = &ctx->sync_maps.entries[i];
+      if (!entry->external) {
+         i++;
+         continue;
+      }
+      npt_external_map_close(entry);
+      npt_sync_map_remove(ctx, entry);
+   }
+}
+
+HRESULT
+npt_resource_bind_d3d11_buffer_shmem(struct npt_context *ctx,
+                                     uint64_t buffer_id,
+                                     uint32_t shmem_res_id,
+                                     uint64_t byte_size,
+                                     uint32_t cookie,
+                                     uint32_t flags)
+{
+   struct npt_d3d_library *lib = npt_renderer_get_library();
+   if (!lib || lib->backend != NPT_BACKEND_DXMT ||
+       !lib->pfn_dxmt_d3d11_buffer_bind_external_fd)
+      return NPT_E_NOTIMPL;
+
+   void *buffer = npt_context_lookup_object_addref(
+      ctx, buffer_id, NPT_OBJECT_TYPE_ID3D11BUFFER);
+   if (!buffer)
+      return NPT_E_INVALIDARG;
+
+   struct npt_resource *shmem =
+      npt_context_pin_resource(ctx, shmem_res_id);
+   if (!shmem || shmem->fd_type != VIRGL_RESOURCE_FD_SHM ||
+       shmem->u.fd < 0 || !byte_size || byte_size > shmem->size) {
+      if (shmem)
+         npt_context_unpin_resource(ctx, shmem);
+      npt_com_release(buffer);
+      return NPT_E_INVALIDARG;
+   }
+
+   HRESULT hr = (HRESULT)lib->pfn_dxmt_d3d11_buffer_bind_external_fd(
+      buffer, shmem->u.fd, shmem->size, cookie, flags);
+   npt_context_unpin_resource(ctx, shmem);
+   npt_com_release(buffer);
+   return hr;
+}
+
 HRESULT
 npt_resource_map(struct npt_context *ctx,
                  uint64_t context_id, uint64_t resource_id,
@@ -224,11 +295,99 @@ npt_resource_map(struct npt_context *ctx,
                  uint32_t mip_height, uint32_t mip_depth,
                  uint32_t shmem_offset,
                  uint32_t *out_row_pitch, uint32_t *out_depth_pitch,
-                 uint32_t *out_mapped_size)
+                 uint32_t *out_mapped_size,
+                 uint32_t *out_external_cookie)
 {
    *out_row_pitch = 0;
    *out_depth_pitch = 0;
    *out_mapped_size = 0;
+   if (out_external_cookie)
+      *out_external_cookie = NPT_EXTERNAL_COOKIE_NONE;
+
+   /* External buffers still call real DXMT Map so DISCARD rename and
+    * deferred-context state stay authoritative in DXMT.  Only the CPU
+    * payload bypasses the shadow copy. */
+   if (context_id) {
+      struct npt_d3d_library *lib = npt_renderer_get_library();
+      if (lib && lib->backend == NPT_BACKEND_DXMT &&
+          lib->pfn_dxmt_d3d11_buffer_is_external &&
+          lib->pfn_dxmt_d3d11_buffer_external_cookie) {
+         void *external_resource = npt_context_lookup_object_addref(
+            ctx, resource_id, NPT_OBJECT_TYPE_IUNKNOWN);
+         if (external_resource &&
+             lib->pfn_dxmt_d3d11_buffer_is_external(external_resource)) {
+            /* D3D11 buffers have only subresource 0 and cannot be mapped
+             * twice.  Reject before calling DXMT Map so we never orphan an
+             * older native Map. */
+            if (npt_sync_map_find(ctx, resource_id, subresource)) {
+               npt_com_release(external_resource);
+               return NPT_E_FAIL;
+            }
+
+            void *external_ctx = npt_context_lookup_object_addref(
+               ctx, context_id, NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
+            if (!external_ctx) {
+               npt_com_release(external_resource);
+               return NPT_E_FAIL;
+            }
+
+            D3D11_MAPPED_SUBRESOURCE mapped = { 0 };
+            PFN_ID3D11DeviceContext_Map map_fn =
+               NPT_COM_VTBL_FUNC(PFN_ID3D11DeviceContext_Map,
+                                 npt_com_vtable(external_ctx),
+                                 NPT_VTBL_ID3D11DeviceContext_Map);
+            HRESULT hr = map_fn(
+               external_ctx, external_resource, subresource,
+               npt_access_flags_to_d3d11_map(access_flags),
+               api_map_flags, &mapped);
+            if (NPT_FAILED(hr)) {
+               npt_com_release(external_ctx);
+               npt_com_release(external_resource);
+               return hr;
+            }
+
+            uint32_t cookie = NPT_EXTERNAL_COOKIE_NONE;
+            const bool have_cookie = mapped.pData &&
+               lib->pfn_dxmt_d3d11_buffer_external_cookie(
+                  external_resource, mapped.pData, &cookie) == 0;
+            struct npt_sync_map_entry *entry =
+               have_cookie ? npt_sync_map_add(ctx) : NULL;
+            if (!entry) {
+               /* A successful DXMT Map must always be paired, including
+                * guest-cookie mismatch and host bookkeeping OOM. */
+               struct npt_sync_map_entry temp = {
+                  .subresource = subresource,
+                  .external = true,
+                  .external_ctx = external_ctx,
+                  .external_resource = external_resource,
+               };
+               npt_external_map_close(&temp);
+               return have_cookie ? NPT_E_OUTOFMEMORY : NPT_E_FAIL;
+            }
+
+            *entry = (struct npt_sync_map_entry){
+               .resource_id = resource_id,
+               .subresource = subresource,
+               .access_flags = access_flags,
+               .mapped_data = mapped.pData,
+               .mapped_size = 0,
+               .persistent = false,
+               .external = true,
+               .external_ctx = external_ctx,
+               .external_resource = external_resource,
+            };
+
+            *out_row_pitch = mapped.RowPitch;
+            *out_depth_pitch = mapped.DepthPitch;
+            *out_mapped_size = byte_size <= UINT32_MAX ? (uint32_t)byte_size : 0;
+            if (out_external_cookie)
+               *out_external_cookie = cookie;
+            return hr;
+         }
+         if (external_resource)
+            npt_com_release(external_resource);
+      }
+   }

    void *resource = npt_context_lookup_object(ctx, NULL, resource_id,
                                               NPT_OBJECT_TYPE_IUNKNOWN);
@@ -240,17 +399,24 @@ npt_resource_map(struct npt_context *ctx,
                                     "map_resource"))
       return NPT_E_INVALIDARG;

+   /* CTX_DETACH_RESOURCE is not ordered against ring commands.
+    * Keep the SHM backing alive until this MAP_RESOURCE command has
+    * finished reading/writing the transport slot. */
    struct npt_resource *shmem_res =
-      npt_context_get_resource(ctx, shmem_res_id);
+      npt_context_pin_resource(ctx, shmem_res_id);
    if (!shmem_res || shmem_res->fd_type != VIRGL_RESOURCE_FD_SHM ||
        !shmem_res->u.data) {
+      if (shmem_res)
+         npt_context_unpin_resource(ctx, shmem_res);
       npt_log("map_resource: invalid SHM resource %u", shmem_res_id);
       return NPT_E_FAIL;
    }

    if ((uint64_t)shmem_offset >= shmem_res->size) {
+      const size_t shmem_size = shmem_res->size;
+      npt_context_unpin_resource(ctx, shmem_res);
       npt_log("map_resource: shmem_offset %u exceeds shmem size %zu",
-              shmem_offset, shmem_res->size);
+              shmem_offset, shmem_size);
       return NPT_E_FAIL;
    }

@@ -275,13 +441,17 @@ npt_resource_map(struct npt_context *ctx,
                            npt_com_vtable(resource),
                            NPT_VTBL_ID3D12Resource_Map);
       if (!map12) {
+         npt_context_unpin_resource(ctx, shmem_res);
          npt_log("map_resource: no D3D12 Map");
          return NPT_E_FAIL;
       }
       hr = map12(resource, subresource, rr, &pData);
-      if (NPT_FAILED(hr))
+      if (NPT_FAILED(hr)) {
+         npt_context_unpin_resource(ctx, shmem_res);
          return hr;
+      }
       if (!pData) {
+         npt_context_unpin_resource(ctx, shmem_res);
          npt_log("map_resource: D3D12 Map returned NULL data");
          return NPT_E_FAIL;
       }
@@ -290,6 +460,7 @@ npt_resource_map(struct npt_context *ctx,
       void *imm_ctx = npt_context_lookup_object(ctx, NULL, context_id,
                                                 NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
       if (!imm_ctx) {
+         npt_context_unpin_resource(ctx, shmem_res);
          npt_log("map_resource: NULL immediate context");
          return NPT_E_FAIL;
       }
@@ -305,8 +476,10 @@ npt_resource_map(struct npt_context *ctx,
       hr = map_fn(imm_ctx, resource, subresource,
                   d3d11_map_type, api_map_flags, &mapped);

-      if (NPT_FAILED(hr))
+      if (NPT_FAILED(hr)) {
+         npt_context_unpin_resource(ctx, shmem_res);
          return hr;
+      }
    }

    /* Memcpy bound for READ (and matching WRITE on the Unmap path):
@@ -342,6 +515,7 @@ npt_resource_map(struct npt_context *ctx,
    } else {
       entry = npt_sync_map_add(ctx);
       if (!entry) {
+         npt_context_unpin_resource(ctx, shmem_res);
          npt_log("map_resource: sync map table OOM");
          return NPT_E_OUTOFMEMORY;
       }
@@ -358,6 +532,8 @@ npt_resource_map(struct npt_context *ctx,
    *out_row_pitch = mapped.RowPitch;
    *out_depth_pitch = mapped.DepthPitch;
    *out_mapped_size = mapped_size;
+
+   npt_context_unpin_resource(ctx, shmem_res);
    return hr;
 }

@@ -370,6 +546,16 @@ npt_resource_unmap(struct npt_context *ctx,
                    uint64_t written_range_begin,
                    uint64_t written_range_end)
 {
+   /* Use the exact pinned objects from Map.  Guest COM_RELEASE may have
+    * removed either id from the object table while the CPU map was live. */
+   struct npt_sync_map_entry *external_entry =
+      npt_sync_map_find(ctx, resource_id, subresource);
+   if (external_entry && external_entry->external) {
+      npt_external_map_close(external_entry);
+      npt_sync_map_remove(ctx, external_entry);
+      return NPT_S_OK;
+   }
+
    void *resource = npt_context_lookup_object(ctx, NULL, resource_id,
                                               NPT_OBJECT_TYPE_IUNKNOWN);
    if (!resource) {
@@ -380,36 +566,43 @@ npt_resource_unmap(struct npt_context *ctx,
                                     "unmap_resource"))
       return NPT_E_INVALIDARG;

+   /* CTX_DETACH_RESOURCE is a virtio control command and is not ordered
+    * against ring commands.  Hold the SHM backing alive until this
+    * UNMAP_RESOURCE command has finished consuming its slot. */
    struct npt_resource *shmem_res =
-      npt_context_get_resource(ctx, shmem_res_id);
+      npt_context_pin_resource(ctx, shmem_res_id);
+
    if (!shmem_res || shmem_res->fd_type != VIRGL_RESOURCE_FD_SHM ||
        !shmem_res->u.data) {
+      if (shmem_res)
+         npt_context_unpin_resource(ctx, shmem_res);
       npt_log("unmap_resource: invalid SHM resource %u", shmem_res_id);
       return NPT_E_FAIL;
    }

-   /* Bounds check the slot window. */
    if ((uint64_t)shmem_offset + byte_size > shmem_res->size) {
+      const size_t shmem_size = shmem_res->size;
+      npt_context_unpin_resource(ctx, shmem_res);
       npt_log("unmap_resource: slot window [%u, %u+%" PRIu64 ") "
               "exceeds shmem size %zu",
-              shmem_offset, shmem_offset, byte_size,
-              shmem_res->size);
+              shmem_offset, shmem_offset, byte_size, shmem_size);
       return NPT_E_FAIL;
    }
+
    const uint8_t *slot_src =
       (const uint8_t *)shmem_res->u.data + shmem_offset;

    if (access_flags) {
-      /* Rename-ring path: no prior MAP_RESOURCE; replay the full
-       * Map + memcpy + Unmap cycle here. */
       if (!context_id) {
+         npt_context_unpin_resource(ctx, shmem_res);
          npt_log("unmap_resource: access_flags path requires context_id");
          return NPT_E_FAIL;
       }

-      void *imm_ctx = npt_context_lookup_object(ctx, NULL, context_id,
-                                                 NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
+      void *imm_ctx = npt_context_lookup_object(
+         ctx, NULL, context_id, NPT_OBJECT_TYPE_ID3D11DEVICECONTEXT);
       if (!imm_ctx) {
+         npt_context_unpin_resource(ctx, shmem_res);
          npt_log("unmap_resource: NULL immediate context");
          return NPT_E_FAIL;
       }
@@ -431,6 +624,7 @@ npt_resource_unmap(struct npt_context *ctx,
       HRESULT hr = map_fn(imm_ctx, resource, subresource,
                           d3d11_map_type, 0, &mapped);
       if (NPT_FAILED(hr)) {
+         npt_context_unpin_resource(ctx, shmem_res);
          npt_log("unmap_resource: rename-ring Map failed 0x%08x", hr);
          return NPT_E_FAIL;
       }
@@ -442,35 +636,36 @@ npt_resource_unmap(struct npt_context *ctx,
          memcpy(mapped.pData, slot_src, (size_t)bound);
       }

+      npt_context_unpin_resource(ctx, shmem_res);
       unmap_fn(imm_ctx, resource, subresource);
       return NPT_S_OK;
    }

-   /* Paired with a prior MAP_RESOURCE. */
    struct npt_sync_map_entry *entry =
       npt_sync_map_find(ctx, resource_id, subresource);
    if (!entry) {
+      npt_context_unpin_resource(ctx, shmem_res);
       npt_log("unmap_resource: resource 0x%" PRIx64 " sub %u not mapped",
               resource_id, subresource);
       return NPT_E_FAIL;
    }

    if (entry->persistent) {
+      npt_context_unpin_resource(ctx, shmem_res);
       npt_log("unmap_resource: resource 0x%" PRIx64 " is persistent, "
               "ignoring unmap", resource_id);
       return NPT_E_FAIL;
    }

    if (entry->access_flags & NPT_MAP_ACCESS_WRITE) {
-      /* MIN(guest byte_size, recorded mapped_size) so we don't
-       * overrun the D3D region with stale/padded SHM bytes.  0 means
-       * "use the full mapped_size". */
       uint64_t bound = byte_size ? byte_size : entry->mapped_size;
       if (bound > entry->mapped_size)
          bound = entry->mapped_size;
       memcpy(entry->mapped_data, slot_src, (size_t)bound);
    }

+   npt_context_unpin_resource(ctx, shmem_res);
+
    if (!context_id) {
       D3D12_RANGE written_range;
       const D3D12_RANGE *wr = NULL;
diff --git a/src/neptune/npt_resource.h b/src/neptune/npt_resource.h
index 6f55b724..efe02501 100644
--- a/src/neptune/npt_resource.h
+++ b/src/neptune/npt_resource.h
@@ -46,7 +46,19 @@ npt_resource_map(struct npt_context *ctx,
                  uint32_t mip_height, uint32_t mip_depth,
                  uint32_t shmem_offset,
                  uint32_t *out_row_pitch, uint32_t *out_depth_pitch,
-                 uint32_t *out_mapped_size);
+                 uint32_t *out_mapped_size,
+                 uint32_t *out_external_cookie);
+
+HRESULT
+npt_resource_bind_d3d11_buffer_shmem(struct npt_context *ctx,
+                                     uint64_t buffer_id,
+                                     uint32_t shmem_res_id,
+                                     uint64_t byte_size,
+                                     uint32_t cookie,
+                                     uint32_t flags);
+
+/* Context teardown helper; leaves ordinary D3D11/D3D12 sync maps alone. */
+void npt_resource_drain_external_maps(struct npt_context *ctx);

 /* RESOURCE_UNMAP.  written_range_{begin,end} are accepted for the
  * D3D12 path (pWrittenRange) and ignored on D3D11. */
diff --git a/src/neptune/npt_transport_defs.h b/src/neptune/npt_transport_defs.h
index f448f2fa..0a348ee5 100644
--- a/src/neptune/npt_transport_defs.h
+++ b/src/neptune/npt_transport_defs.h
@@ -221,6 +221,11 @@ struct npt_cmd_com_query_interface_reply {
 #define NPT_TRANSPORT_RESOURCE_UNMAP                 2u
 #define NPT_TRANSPORT_RESOURCE_EXECUTE_CMD_STREAM    3u
 #define NPT_TRANSPORT_RESOURCE_CREATE_HEAP_FROM_SHMEM 4u
+#define NPT_TRANSPORT_RESOURCE_BIND_D3D11_BUFFER_SHMEM 5u
+
+#define NPT_D3D11_EXTERNAL_BIND_INITIAL 0x1u
+#define NPT_EXTERNAL_COOKIE_NONE 0xffffffffu
+#define NPT_E_NEED_EXTERNAL_BUFFER_ALLOCATION ((HRESULT)0x8004d101u)

 /* UpdateSubresource(1) wire path; also serves D3D11_SUBRESOURCE_DATA
  * pSysMem.  D3D12 WriteToSubresource has identical shape (same box
@@ -306,7 +311,8 @@ struct npt_cmd_map_resource_reply {
    uint32_t row_pitch;
    uint32_t depth_pitch;
    uint32_t mapped_size;      /* 0 for persistent */
-   uint32_t pad;
+   /* D3D11 external-buffer path: guest-visible backing index. */
+   uint32_t external_cookie;
 };

 /* Synchronous: guest must not submit Draws reading this resource until
@@ -366,6 +372,24 @@ struct npt_cmd_create_heap_from_shmem_reply {
    struct npt_reply_header header; /* header.cmd_return = HRESULT */
 };

+/* Synchronous.  Attach one guest SHM blob as a physical allocation in
+ * a DXMT dynamic ID3D11Buffer.  The backend duplicates the fd before
+ * this command replies, so the virtio resource can subsequently die
+ * independently of the GPU allocation. */
+struct npt_cmd_bind_d3d11_buffer_shmem {
+   struct npt_command_header header;
+   /* header.object_id = ID3D11Buffer guest id */
+   uint32_t shmem_res_id;
+   uint32_t cookie;
+   uint64_t byte_size;
+   uint32_t flags; /* NPT_D3D11_EXTERNAL_BIND_* */
+   uint32_t pad;
+};
+
+struct npt_cmd_bind_d3d11_buffer_shmem_reply {
+   struct npt_reply_header header;
+};
+
 /* ====================================================================== */
 /* SHARED                                                                 */
 /* ====================================================================== */
